<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:ship_debbuger="clr-namespace:Ship_Debbuger" x:DataType="ship_debbuger:MainPageVM"
             x:Class="Ship_Debbuger.MainPage">

    <Grid>
        <Grid.RowDefinitions >
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style x:Key="_defaultLabel" TargetType="Label">
                <Setter Property="FontSize" Value="35"/>
            </Style>
        </Grid.Resources>

        <Label Grid.Row="0" Style="{StaticResource _defaultLabel}" Text="{Binding XVal, Mode=OneWay}"/>
        <Label Grid.Row="1" Style="{StaticResource _defaultLabel}" Text="{Binding YVal, Mode=OneWay}"/>

        <Button Grid.Row="2" Text="ДАЙ" Command="{Binding GetCommand}"/>
    </Grid>

</ContentPage>
